miniMaxSum complexity:
Time: There are two iterators looping over the list once, so the time complexity will be 2n or O(n)
Space: There are only 3 extra variables, none of which depend on the input size, so the space complexity is O(1)